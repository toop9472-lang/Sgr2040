# 🤖 Claude Haiku 4.5 AI Integration Guide

## نظرة عامة

تم تفعيل **Claude Haiku 4.5** من Anthropic لجميع عملاء التطبيق. هذا النموذج المتقدم يوفر قدرات ذكاء اصطناعي قوية مع استهلاك موارد فعال.

## الميزات المدعومة ✨

### 1. **توليد الردود الذكية** 💬
- الرد على أسئلة المستخدمين
- توليد محتوى حسب الطلب
- تخصيص النتائج حسب نوع المستخدم

### 2. **تلخيص النصوص** 📄
- تلخيص محتوى طويل بشكل فعال
- دعم لغات متعددة (العربية، الإنجليزية، إلخ)

### 3. **الترجمة الآلية** 🌐
- ترجمة ثنائية الاتجاه
- دعم لغات متعددة

### 4. **تحليل المحتوى** 🔍
- فحص سلامة المحتوى
- تقييم ملاءمة الإعلانات
- كشف المحتوى غير الملائم

## التكوين 🔧

### متطلبات الإعداد

1. **مفتاح API من Anthropic**
   ```bash
   export ANTHROPIC_API_KEY="your-api-key-here"
   ```

2. **متغيرات البيئة الاختيارية**
   ```bash
   CLAUDE_MAX_TOKENS=1024  # الحد الأقصى للرموز (افتراضي: 1024)
   ```

3. **تفعيل من لوحة المدير**
   - اذهب إلى: **Admin Dashboard → Settings → AI Models**
   - تفعيل "Claude Haiku 4.5"
   - تفعيل "Enable Claude Haiku 4.5 for all clients"
   - احفظ الإعدادات

## نقاط النهاية (Endpoints) 🔌

### 1. توليد الردود
```http
POST /api/claude-ai/generate-response
Content-Type: application/json
Authorization: Bearer {token}

{
  "prompt": "ما هي أفضل ممارسات الأمان الإلكتروني؟",
  "system_message": "أنت خبير أمان إلكتروني",
  "max_tokens": 1024,
  "temperature": 0.7
}
```

**الرد:**
```json
{
  "success": true,
  "response": "أفضل ممارسات الأمان الإلكتروني تشمل...",
  "model": "claude-haiku-4.5",
  "provider": "anthropic",
  "usage": {
    "prompt_tokens": 15,
    "completion_tokens": 200
  }
}
```

### 2. تلخيص النصوص
```http
POST /api/claude-ai/summarize
Content-Type: application/json
Authorization: Bearer {token}

{
  "text": "النص الطويل المراد تلخيصه...",
  "language": "ar"
}
```

### 3. الترجمة
```http
POST /api/claude-ai/translate
Content-Type: application/json
Authorization: Bearer {token}

{
  "text": "مرحبا بك في التطبيق",
  "target_language": "en"
}
```

### 4. تحليل المحتوى (للمسؤولين فقط)
```http
POST /api/claude-ai/analyze-content
Content-Type: application/json
Authorization: Bearer {admin-token}

{
  "content": "محتوى الإعلان المراد تحليله..."
}
```

### 5. معلومات الحالة
```http
GET /api/claude-ai/status
Authorization: Bearer {token}
```

**الرد:**
```json
{
  "enabled": true,
  "enabled_for_all": true,
  "status": "active",
  "model": "claude-haiku-4.5",
  "provider": "anthropic",
  "configured": true,
  "message": "Claude Haiku 4.5 is active"
}
```

## استخدام خدمة Claude في الكود 💻

### Python Backend

```python
from services.claude_ai_service import get_claude_service

# الحصول على الخدمة
claude = get_claude_service()

# توليد رد
result = await claude.generate_response(
    prompt="سؤال المستخدم",
    system_message="تعليمات النموذج",
    temperature=0.7
)

if result['success']:
    print(result['response'])
else:
    print(f"Error: {result['error']}")
```

### Frontend React

```javascript
// استدعاء API
const response = await axios.post(
  `${BACKEND_URL}/api/claude-ai/generate-response`,
  {
    prompt: "سؤال المستخدم",
    system_message: "تعليمات النموذج",
    max_tokens: 1024,
    temperature: 0.7
  },
  {
    headers: {
      Authorization: `Bearer ${token}`
    }
  }
);

console.log(response.data.response);
```

## إعدادات قاعدة البيانات 📊

### مجموعة الإعدادات `ai_models`

```json
{
  "type": "ai_models",
  "claude_haiku_enabled": true,
  "claude_haiku_enabled_for_all_clients": true,
  "model_name": "claude-haiku-4.5",
  "api_provider": "anthropic",
  "max_tokens": 1024,
  "updated_at": "2026-01-26T10:30:00Z"
}
```

## حدود الاستخدام والتسعير 💰

### نموذج Claude Haiku 4.5
- **سرعة**: سريع جداً
- **التكلفة**: منخفضة جداً
- **دقة**: عالية
- **الحالات الاستخدامية**: تلخيص، ترجمة، تحليل سريع

### نصائح التحسين
1. استخدم `temperature` منخفضة (0.3-0.5) للمهام الدقيقة
2. استخدم `temperature` أعلى (0.7-0.9) للمهام الإبداعية
3. حدد `max_tokens` بحد معقول لتوفير التكاليف
4. استخدم `system_message` واضحة لتحسين الدقة

## استكشاف الأخطاء 🐛

### الخطأ: "API key is not configured"
- تأكد من تعيين `ANTHROPIC_API_KEY` في متغيرات البيئة
- تحقق من صحة المفتاح

### الخطأ: "Claude Haiku AI is not enabled"
- فعّل الميزة من لوحة المدير
- تحقق من إعدادات قاعدة البيانات

### الخطأ: "Access denied"
- تحقق من أن العملاء مصرح لهم باستخدام الميزة
- تأكد من تفعيل الميزة لجميع العملاء من لوحة المدير

## الأمان والخصوصية 🔒

1. **لا تشارك المفاتيح العامة**
   - احفظ `ANTHROPIC_API_KEY` بأمان
   - لا ترفعه على GitHub

2. **التحقق من الموثقية**
   - جميع طلبات API تتطلب مصادقة
   - استخدم JWT tokens من نظام المصادقة

3. **فحص المحتوى**
   - استخدم `/analyze-content` لفحص الإعلانات
   - طبق سياسات قبول المحتوى

## الدعم والتطوير 📞

للمساعدة أو الإبلاغ عن مشاكل:
- تواصل مع فريق الدعم
- راجع الوثائق الرسمية: https://docs.anthropic.com/

---

**آخر تحديث:** يناير 2026  
**الإصدار:** 1.0  
**الحالة:** ✅ نشط
