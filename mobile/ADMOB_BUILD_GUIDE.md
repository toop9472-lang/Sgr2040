# دليل بناء تطبيق صقر مع AdMob على MacBook السحابي 🦅

## المتطلبات الأساسية

### بيانات AdMob (جاهزة ✅)
| النظام | App ID | Rewarded Ad Unit ID |
|--------|--------|---------------------|
| iOS | `ca-app-pub-5132559433385403~6910358704` | `ca-app-pub-5132559433385403/2999033852` |
| Android | `ca-app-pub-5132559433385403~6910358704` | `ca-app-pub-5132559433385403/3389052725` |

---

## الخطوات على MacBook السحابي

### الخطوة 1: فتح Terminal
1. افتح **Finder**
2. اذهب إلى **Applications** → **Utilities** → **Terminal**
   - أو اضغط `Cmd + Space` واكتب `Terminal`

---

### الخطوة 2: تثبيت المتطلبات

افتح Terminal وانسخ الأوامر التالية واحدة تلو الأخرى:

```bash
# التحقق من Node.js
node --version
# إذا لم يكن موجوداً، ثبته من: https://nodejs.org

# تثبيت EAS CLI
npm install -g eas-cli

# التحقق من التثبيت
eas --version
```

---

### الخطوة 3: تسجيل الدخول لـ Expo

```bash
eas login
```

سيطلب منك:
- **Email:** أدخل بريدك الإلكتروني المسجل في Expo
- **Password:** أدخل كلمة المرور

للتحقق:
```bash
eas whoami
```

---

### الخطوة 4: تحميل المشروع

**الطريقة 1: عبر GitHub**
```bash
cd ~/Desktop
git clone https://github.com/YOUR_USERNAME/saqr-app.git
cd saqr-app/mobile
```

**الطريقة 2: عبر رفع الملفات**
1. ارفع مجلد `mobile` إلى Google Drive أو Dropbox
2. حمله على الـ Mac
3. افتح Terminal وانتقل للمجلد:
```bash
cd ~/Downloads/mobile
```

---

### الخطوة 5: تثبيت المكتبات

```bash
# تثبيت yarn إذا لم يكن موجوداً
npm install -g yarn

# تثبيت مكتبات المشروع
yarn install

# أو باستخدام npm
npm install
```

---

### الخطوة 6: بناء تطبيق iOS 🍎

```bash
# بناء للـ App Store
eas build --platform ios --profile production
```

سيطلب منك:
1. **Apple ID:** أدخل بريدك الإلكتروني لـ Apple
2. **Password:** كلمة مرور Apple ID
3. **Team:** اختر فريق المطور الخاص بك
4. **Bundle Identifier:** اضغط Enter (سيستخدم الافتراضي)

**انتظر حتى يكتمل البناء** (قد يستغرق 15-30 دقيقة)

عند الانتهاء، ستحصل على رابط لتحميل ملف `.ipa`

---

### الخطوة 7: رفع التطبيق لـ App Store Connect

```bash
# رفع تلقائي
eas submit --platform ios
```

**أو يدوياً باستخدام Transporter:**
1. افتح **Transporter** من Applications
2. سجل دخول بـ Apple ID
3. اسحب ملف `.ipa` إلى النافذة
4. اضغط **Deliver**

---

### الخطوة 8: بناء تطبيق Android 🤖

```bash
# بناء APK للاختبار
eas build --platform android --profile preview

# أو بناء AAB للمتجر
eas build --platform android --profile production
```

---

## حل المشاكل الشائعة

### مشكلة: خطأ في تسجيل الدخول لـ Apple
```bash
# أعد تسجيل الدخول
eas logout
eas login
```

### مشكلة: فشل البناء بسبب الشهادات
```bash
# أعد إنشاء الشهادات
eas credentials
# اختر iOS → Remove existing credentials → Yes
# ثم أعد البناء
eas build --platform ios --profile production
```

### مشكلة: خطأ في CocoaPods
```bash
cd ios
pod install --repo-update
cd ..
```

### مشكلة: خطأ في node_modules
```bash
rm -rf node_modules
rm yarn.lock
yarn install
```

---

## ملفات مهمة

| الملف | الوصف |
|-------|-------|
| `app.json` | إعدادات التطبيق و AdMob |
| `package.json` | المكتبات المطلوبة |
| `eas.json` | إعدادات البناء |
| `src/services/admobService.js` | خدمة الإعلانات |
| `certificates/` | شهادات Apple |

---

## اختبار الإعلانات

### في وضع التطوير (`__DEV__ = true`)
- يستخدم إعلانات تجريبية من Google
- لن يؤثر على حساب AdMob

### في وضع الإنتاج (`__DEV__ = false`)
- يستخدم إعلاناتك الحقيقية
- تأكد من مراجعة Google لتطبيقك

---

## معلومات الاتصال للدعم

إذا واجهت أي مشكلة:
1. التقط صورة للخطأ
2. انسخ نص الخطأ من Terminal
3. أرسلها للمطور

---

## قائمة التحقق النهائية ✅

- [ ] تسجيل الدخول لـ Expo
- [ ] تثبيت المكتبات
- [ ] بناء iOS بنجاح
- [ ] رفع للـ App Store Connect
- [ ] بناء Android بنجاح
- [ ] اختبار الإعلانات

---

**تم إنشاء هذا الدليل في:** ديسمبر 2025
**إصدار التطبيق:** 4.6.0
