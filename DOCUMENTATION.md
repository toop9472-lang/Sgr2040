# 📱 تطبيق صقر - التوثيق الفني الكامل

## نظرة عامة
تطبيق صقر هو منصة متكاملة لربح المال من خلال مشاهدة الإعلانات، مبني بتقنيات حديثة ويدعم iOS و Android.

---

## 🏗️ البنية التقنية

### Frontend (تطبيق الموبايل)
```
التقنية: React Native + Expo SDK 52
اللغة: JavaScript
```

### Backend (الخادم)
```
التقنية: FastAPI (Python)
قاعدة البيانات: MongoDB
المصادقة: JWT Tokens
```

---

## 📁 هيكل المشروع

```
/app
├── backend/                 # خادم API
│   ├── auth/               # نظام المصادقة
│   │   ├── jwt_handler.py  # إدارة JWT
│   │   ├── password_utils.py # أمان كلمات المرور
│   │   ├── rate_limiter.py # حماية Brute Force
│   │   └── encryption.py   # تشفير البيانات
│   ├── routes/             # نقاط API
│   │   ├── auth_routes.py  # تسجيل/دخول
│   │   ├── ad_routes.py    # الإعلانات
│   │   ├── user_routes.py  # المستخدمين
│   │   └── ...             # 29 ملف routes
│   ├── models/             # نماذج البيانات
│   └── server.py           # الخادم الرئيسي
│
├── mobile/                  # تطبيق الموبايل
│   ├── src/
│   │   ├── screens/        # الشاشات
│   │   ├── components/     # المكونات
│   │   └── services/       # خدمات API
│   ├── App.js              # نقطة البداية
│   ├── app.json            # إعدادات Expo
│   └── package.json        # المكتبات
│
└── frontend/               # لوحة الإدارة (Web)
    └── src/
```

---

## 🔐 نظام الأمان

### المصادقة (Authentication)
- JWT Access Token (24 ساعة)
- JWT Refresh Token (30 يوم)
- تجديد تلقائي للتوكن

### حماية كلمات المرور
- تشفير bcrypt
- حد أدنى 8 أحرف
- حرف كبير + صغير + رقم
- منع كلمات المرور الشائعة

### حماية Brute Force
- 5 محاولات فاشلة = قفل 15 دقيقة
- تسجيل IP وUser Agent
- إلغاء قفل تلقائي عند النجاح

### تشفير البيانات
- AES للبيانات المالية
- إخفاء الأرقام الحساسة

---

## 📊 قاعدة البيانات (MongoDB)

### Collections الرئيسية:
```javascript
users {
  id: string,
  email: string,
  name: string,
  password_hash: string,
  points: number,
  total_earned: number,
  status: string,
  created_at: datetime
}

ads {
  id: string,
  title: string,
  description: string,
  video_url: string,
  advertiser: string,
  duration: number,
  points_per_view: number
}

withdrawals {
  id: string,
  user_id: string,
  amount: number,
  points: number,
  method: string,
  status: string
}

login_attempts {
  email: string,
  success: boolean,
  ip_address: string,
  timestamp: datetime
}
```

---

## 🔌 API Endpoints

### المصادقة
```
POST /api/auth/signin          # تسجيل الدخول
POST /api/auth/register        # إنشاء حساب
POST /api/auth/refresh-token   # تجديد التوكن
GET  /api/auth/me              # بيانات المستخدم
```

### الإعلانات
```
GET  /api/ads                  # قائمة الإعلانات
POST /api/rewarded-ads/complete # إكمال مشاهدة
```

### السحب
```
GET  /api/withdrawals          # طلبات السحب
POST /api/withdrawals          # طلب سحب جديد
```

---

## 📱 شاشات التطبيق

1. **AuthScreen** - تسجيل الدخول/إنشاء حساب
2. **HomeScreen** - الصفحة الرئيسية والإحصائيات
3. **AdViewerScreen** - مشاهدة الإعلانات
4. **ProfileScreen** - الملف الشخصي
5. **AdvertiserScreen** - لوحة المعلنين

---

## 🎯 الميزات

### للمستخدمين:
- ✅ مشاهدة إعلانات وكسب نقاط
- ✅ إعلانات Google AdMob المكافئة
- ✅ تحديات يومية
- ✅ سحب الأرباح
- ✅ محادثة AI ذكية

### للمعلنين:
- ✅ لوحة إعلانات خاصة
- ✅ إدارة الحملات
- ✅ إحصائيات المشاهدات

### للمسؤولين:
- ✅ لوحة إدارة كاملة
- ✅ إدارة المستخدمين
- ✅ مراجعة طلبات السحب
- ✅ تقارير وإحصائيات

---

## 🚀 النشر

### iOS
- Bundle ID: `com.saqr.rewards`
- App Store Connect ID: `6758868843`

### Android
- Package: `com.saqr.rewards`
- Google Play Console

---

## 📈 الإصدارات

| الإصدار | التاريخ | التغييرات |
|---------|---------|-----------|
| 4.6.0 | فبراير 2026 | إضافة AdMob + تحسينات أمنية |
| 4.5.0 | يناير 2026 | الإصدار الأولي |

---

## 🛠️ التشغيل للمطورين

### Backend
```bash
cd backend
pip install -r requirements.txt
uvicorn server:app --reload
```

### Mobile
```bash
cd mobile
yarn install
npx expo start
```

### Build
```bash
# iOS
npx eas-cli build --platform ios --profile production

# Android
npx eas-cli build --platform android --profile production
```

---

## 📞 الدعم الفني
للمساعدة أو الاستفسارات، تواصل مع فريق التطوير.
